CREATE TABLE `scta`.`positions` (
  `Id` INT NOT NULL AUTO_INCREMENT,
  `positionName` VARCHAR(45) NOT NULL,
  `positionEmail` VARCHAR(45) NOT NULL,
  `volunteerName` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`Id`));